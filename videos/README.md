# Video message

Drop your local video file here named **`message.mp4`**, then in `index.html`
set the `#video` section attribute `data-show-local="true"`.

Prefer YouTube instead? Leave this empty and set `data-youtube-id="YOUR_ID"`
on the same section. You can use both at once.
